/**
 * Default `sys_role.value` assigned on email sign-up when the role row exists.
 * Not better-auth's role field — this is application RBAC in `sys_role` / `sys_user_role`.
 */
export const DEFAULT_SIGNUP_ROLE_VALUE = "user";
