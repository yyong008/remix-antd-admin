import styled from "styled-components";

const ContentDiv = styled.div`
  background: #fff;
  padding: 10px 20px;
`;

const TDiv = styled.div`
  padding: 0px 0px 20px 0px;
  color: rgba(0, 0, 0, 0.88);
  font-weight: 600;
  font-size: 16px;
  line-height: 1.5;
`;

export { ContentDiv, TDiv };
