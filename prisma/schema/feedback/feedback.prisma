model FeedBack {
  id        Int       @id @default(autoincrement())
  userId    Int
  content   String
  url       String?
  createdAt DateTime  @default(now())
  updatedAt DateTime?

  @@map("feed_back")
}
