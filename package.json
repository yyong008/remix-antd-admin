{
  "name": "react-router-antd-admin",
  "version": "0.2.0",
  "description": "React Router Antd Admin",
  "keywords": [
    "React Router",
    "admin",
    "ai",
    "antd",
    "hono",
    "ollama",
    "tailwindcss"
  ],
  "homepage": "https://remix-antd-admin.vercel.app/",
  "license": "MIT",
  "author": {
    "name": "yong",
    "email": "yyong008@outlook.com",
    "url": "https://github.com/yyong008"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/yyong008/remix-antd-admin.git"
  },
  "type": "module",
  "scripts": {
    "build": "turbo build",
    "coverage": "turbo test run --coverage",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "turbo fmt",
    "test": "turbo test",
    "commit": "git-cz",
    "changeset": "changeset",
    "version": "changeset version",
    "drizzle:generate": "pnpm -F @workspace/database run generate",
    "auth:generate:schema": "pnpm dlx auth@latest generate --config=./packages/database/src/seed/local/auth.ts --output=./packages/database/src/schema/auth.ts",
    "drizzle:push": "pnpm -F @workspace/database run push",
    "drizzle:migrate": "pnpm -F @workspace/database run migrate",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "db:setup:local": "pnpm run db:migrate:local && pnpm run db:seed:local",
    "db:generate:seed-sql": "pnpm -F @workspace/database run generate:seed-sql",
    "db:seed:local": "pnpm exec wrangler d1 execute DB --local --file=./packages/database/src/seed/local/seed-local-rbac.sql",
    "db:seed": "pnpm run db:seed:local",
    "db:studio:local": "pnpm -F @workspace/database run studio",
    "db:generate:sqlite_file": "wrangler d1 execute DB --local --command \"SELECT 1\"",
    "cf:size": "pnpm run build && wrangler deploy --dry-run"
  },
  "dependencies": {},
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@types/node": "catalog:",
    "commitizen": "^4.3.1",
    "cz-conventional-changelog": "^3.3.0",
    "dotenv-cli": "^11.0.0",
    "rimraf": "^6.1.3",
    "tsx": "^4.21.0",
    "turbo": "catalog:",
    "typescript": "catalog:"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  },
  "packageManager": "pnpm@10.33.0"
}